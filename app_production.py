#!/usr/bin/env python3
import os
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddlewareapp = FastAPI(title="VortexAI", version="1.0")app.add_middleware(    CORSMiddleware,    allow_origins=["*"],    allow_credentials=True,    allow_methods=["*"],    allow_headers=["*"],)deals = {}@app.get("/health")async def health():    return {"status": "healthy", "service": "vortexai"}@app.post("/admin/webhooks/deal-ingest")async def ingest_deal(data: dict):    deal_id = len(deals) + 1    deals[deal_id] = data    return {"status": "success", "id": deal_id}@app.get("/admin/deals")async def get_deals():    return {"deals": list(deals.values()), "total": len(deals)}@app.get("/")async def root():    return {"status": "online"}if __name__ == "__main__":    import uvicorn    uvicorn.run(app, host="0.0.0.0", port=int(os.getenv("PORT", 8080)))
